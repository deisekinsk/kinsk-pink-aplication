SQL STATEMENT PINK APP


// To organize by PRICING_CATEGORY_ID

SELECT PRICING_CATEGORY_ID, COUNT(*) AS TOTAL
FROM SUBSCRIPTION
GROUP BY PRICING_CATEGORY_ID;



SELECT
    P.PRICING_CATEGORY_ID,
    P.NAME,
    COUNT(*) AS TOTAL
FROM
    SUBSCRIPTION S
JOIN
    PRICING_CATEGORY P ON S.PRICING_CATEGORY_ID = P.PRICING_CATEGORY_ID
GROUP BY
    P.PRICING_CATEGORY_ID, P.NAME;


// To organize by SUBS_STATUS

SELECT SUBS_STATUS, COUNT(*) AS TOTAL
FROM SUBSCRIPTION
GROUP BY SUBS_STATUS;

// Retrieve subscriptions with different start and last update dates.
//displaying status and end date.


SELECT
    START_DATE,
    LAST_UPDATE,
    SUBS_STATUS,
    END_DATE,
    COUNT(*) AS TOTAL
FROM
    SUBSCRIPTION
WHERE
    START_DATE <> LAST_UPDATE
GROUP BY
    START_DATE, LAST_UPDATE, SUBS_STATUS, END_DATE;

// To insert in PRICING_CATEGORY

-- Categoria 1
INSERT INTO PRICING_CATEGORY (NAME, FIXED_RATE, DISCOUNT, USAGE_RATE) VALUES ('Category 1', 10.0, 5.0, 0.5);

-- Categoria 2
INSERT INTO PRICING_CATEGORY (NAME, FIXED_RATE, DISCOUNT, USAGE_RATE) VALUES ('Category 2', 12.0, 7.0, 0.6);

-- Categoria 3
INSERT INTO PRICING_CATEGORY (NAME, FIXED_RATE, DISCOUNT, USAGE_RATE) VALUES ('Category 3', 15.0, 8.0, 0.7);

-- Categoria 4
INSERT INTO PRICING_CATEGORY (NAME, FIXED_RATE, DISCOUNT, USAGE_RATE) VALUES ('Category 4', 18.0, 10.0, 0.8);

-- Categoria 5
INSERT INTO PRICING_CATEGORY (NAME, FIXED_RATE, DISCOUNT, USAGE_RATE) VALUES ('Category 5', 20.0, 12.0, 1.0);
